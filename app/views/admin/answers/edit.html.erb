<div class="panel panel-default">
  <div class="panel-heading">
    答案修改
  </div>
  <div class="panel-body">
    <%= form_for [:admin, @question, @answer] do |f| %> 
      <%= f.label :content, '内容' %>
      <%= f.text_field :content, class: 'form-control' %>
      <label>发布者</label>
      <input class="form-control" type="text" readonly="value" value="<%= @answer.user.name %>">
      <%= f.submit '保存修改', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
