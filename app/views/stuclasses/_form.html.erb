<%= form_for @stuclass do |f| %>
<%= render "shared/error_messages", object: f.object %>
<div class="form-group">
  <%= f.text_field :scname, class: "form-control", placeholder: "在这里填写班级名称" %>
</div>
<div class="form-group">
  <%= f.submit "添加班级", class: "btn btn-primary" %>
</div>
<% end %>
